# ── Web framework ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── AI / LLM (optional — app works without this) ──
google-generativeai>=0.8.0
tenacity==8.5.0

# ── Document generation ──
pypdf==5.0.0
fpdf2==2.7.9
python-docx==1.1.0

# ── HTTP / networking (IntaSend M-Pesa STK push) ──
requests>=2.31.0
httpx>=0.27.0

# ── Utilities ──
python-dotenv==1.0.1
